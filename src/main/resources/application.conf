db-dispatcher {
    type = PinnedDispatcher
    executor = "thread-pool-executor"
}

db {
	host = localhost
	port = 5432
	name = shorty
	user = shorty
	pass = shorty
}

http {
	host = 0.0.0.0
	port = 8080
}
